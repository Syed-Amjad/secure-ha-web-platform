---
- name: restart sshd
  ansible.builtin.systemd:
    name: ssh # Ubuntu names the unit `ssh`, not `sshd`
    state: restarted

- name: restart fail2ban
  ansible.builtin.systemd:
    name: fail2ban
    state: restarted
