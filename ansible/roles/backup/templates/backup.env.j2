# {{ ansible_managed }}
# No AWS credentials here. The instance carries an IAM role, and IMDSv2 is
# enforced — that is the whole point of aws_iam_instance_profile.db.
BACKUP_BUCKET={{ backup_bucket }}
KMS_KEY_ID={{ kms_key_id }}
